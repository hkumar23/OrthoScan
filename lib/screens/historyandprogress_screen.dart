import 'package:flutter/material.dart';

class HistoryAndProgress extends StatelessWidget {
  static const routeName='/historyandprogress-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("History and Progress"),
      ),
    );
  }
}